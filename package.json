{
  "name": "automation-fleet-manager",
  "version": "1.0.0",
  "description": "Projeto de automação utilizando o cypress para um sistema de gestão de frotas",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^15.0.0",
    "cypress-cucumber-preprocessor": "^4.3.1"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": "cypress/support/steps/*/*.{js,ts}",
    "filterSpecs": true,
    "omitFiltered": true,
    "nonGlobalStepDefinitions": true
  },
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^23.0.0"
  }
}
